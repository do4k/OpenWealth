# Copy this file to .env (next to docker-compose.yml) and fill it in.
# docker compose reads .env automatically.

# REQUIRED. JWT signing key, 32+ characters. Generate one with:
#   openssl rand -base64 48
# Keep it stable across restarts or everyone gets logged out.
JWT__KEY=

# Optional. Timezone inside the container.
TZ=Europe/London
